# 実装予定の内容

## TODOリスト
- [ ] **verが変わった時に差分を検知して出力する機能**
  - YAMLファイルの `version` フィールドを監視。
  - 差分を検知し、変更内容をログや別ファイルに出力する仕組みを実装。

- [ ] **in/outからyamlを書き換える機能**
  - 入力（in）データを受け取り、YAMLファイルの内容を更新。
  - 出力（out）データとして、更新後のYAMLを保存する仕組みを実装。
  - 必要に応じて、バリデーションやエラーハンドリングを追加。

## メモ
- 差分検知には `PyYAML` を使用してYAMLファイルを読み込み、比較する。
- YAMLの書き換えには、Pythonの標準ライブラリや `PyYAML` を活用する。
- 必要に応じて、CLIツールとして操作できるようにする。